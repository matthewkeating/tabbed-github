{
  "name": "tabbed-github",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "tauri": "tauri",
    "dev:gemini": "tauri dev --features gemini --config src-tauri/tauri.gemini.conf.json",
    "deploy": "bash scripts/deploy.sh github",
    "deploy:gemini": "bash scripts/deploy.sh gemini"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
